<h1><%= @resume.title %> 履歷</h1>
<%= link_to "修改", edit_resume_path(@resume) %>

<%= markdown(@resume.content) %>
<%= image_tag @resume.mugshot.variant(resize_to_limit: [300,300]) if @resume.mugshot.attached? %>